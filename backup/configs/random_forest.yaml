experiment_name: rf_experiment

model_name: random_forest

model:
  n_estimators: 100
  max_depth: 10

training:
  test_size: 0.2
  random_state: 42

optimization:
  enabled: true
  type: quantization
